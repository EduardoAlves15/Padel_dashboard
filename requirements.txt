# Deep Learning & Computer Vision
ultralytics>=8.0.0          # YOLOv8 detection and pose estimation
torch>=2.0.0                # PyTorch
torchvision>=0.15.0         # Vision utilities
opencv-python>=4.8.0        # OpenCV for stereo vision and calibration
opencv-contrib-python>=4.8.0  # Extra OpenCV modules (stereo calibration, etc.)
opencv-python-headless>=4.8.0  # OpenCV without GUI dependencies (for server environments)
# Tracking
lap>=0.4.0                  # Linear Assignment Problem solver (used by ByteTrack)
filterpy>=1.4.5             # Kalman filtering for tracking

# Action Recognition
pytorchvideo>=0.1.5         # SlowFast and other video models

# Data Processing & Visualization
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Evaluation
motmetrics>=1.4.0           # MOT evaluation metrics (MOTA, MOTP, IDF1, etc.)
scikit-learn>=1.2.0         # Classification metrics (confusion matrix, F1, etc.)

# Utilities
pyyaml>=6.0
tqdm>=4.65.0
Pillow>=9.5.0

# Jupyter
jupyter>=1.0.0
ipywidgets>=8.0.0

# Dashboard
streamlit>=1.36.0
